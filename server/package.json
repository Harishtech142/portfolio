{
  "name": "TrexFlow-contact-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Standalone contact-form backend for the TrexFlow portfolio. Receives contact submissions, emails them to the site owner via Gmail SMTP (Nodemailer), and sends an automatic confirmation email to the visitor.",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "nodemailer": "^6.9.14"
  }
}
